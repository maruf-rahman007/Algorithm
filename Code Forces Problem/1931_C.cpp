#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        int arr[n];
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
        bool flag = false;
        int count = 0;
        int low = 0;
        int high = n - 1;
        while (low < high)
        for (int i = 0; i < n; i++)
        {
            
        }
        
        cout<<count<<endl;
    }

    return 0;
}